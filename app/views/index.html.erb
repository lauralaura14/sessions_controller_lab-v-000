<%= form_for(:user, :controller => "sessions", :action => "destroy", :html => {:method => "delete"}) do |f| %>

  <%= f.label :name %><br/>
  <%= f.name_field :name %>

  <%= f.submit "Logout" %><br/>

<% end %>
